services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: bug-backend
    ports:
      - "5001:5001"

  frontend:
    build:
      context: ./frontend
    container_name: bug-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

      